"use strict";
let myUsername = ['Sunny', 'Faisal', 'Humair', 'Arbaaz', 'Admin', 'Salman'];
if (myUsername.length === 0) {
    console.log('Username Is not Empty');
}
else {
    myUsername = [];
    console.log(`We need to find some users! ${myUsername.length}`);
}
